[package]
name = "fibonacci-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
alloy-sol-types = { workspace = true }
serde = { version = "1.0.210", default-features = false, features = ["derive", "alloc"]}
